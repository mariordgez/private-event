<%= form_with(model: attendee) do |form| %>
  <% if attendee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(attendee.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
        <% attendee.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :attendee %>
    <%= form.text_field :userame, id: :userame, value: current_user.email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
